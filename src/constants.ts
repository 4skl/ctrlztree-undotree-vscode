export const DIFF_SCHEME = 'ctrlztree-diff';
export const ACTION_TIMEOUT = 500;
export const PAUSE_THRESHOLD = 1500;
export const MAX_HISTORY_NODES_PER_DOCUMENT = 1000; // Limit history depth to prevent unbounded growth
export const MAX_TOTAL_DOCUMENTS = 100; // Maximum number of documents to track simultaneously
